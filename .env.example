# Copy to .env and replace both values with separate random secrets.
# Or run ./scripts/prepare-compose.ps1 to generate them without printing secrets.
POSTGRES_PASSWORD=
WEAVE_TOKEN_SECRET=
WEAVE_HTTP_PORT=5188
WEAVE_ORIGINS=http://localhost:5188,http://127.0.0.1:5188
